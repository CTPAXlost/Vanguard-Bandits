VANGUARD BANDITS REMASTER 1.9.6 — ОТЧЁТ ПРОВЕРКИ

Источник ошибки: logs_81873053197.zip
Падающий этап: Blocking boot smoke for missions 1-6
Падающая миссия: II
Файл: res://scripts/battle_prototype.gd
Функция: _create_house
Ошибка: Out of bounds get index '2' при чтении массива size из двух элементов.

Реальная причина:
- mission_02.json хранит размеры домов в старом формате [ширина, глубина];
- высота хранится отдельно в поле height;
- код 1.9.5 без проверки читал size[2], ожидая формат [ширина, высота, глубина].

Исправление:
- новый формат [width, height, depth] поддерживается;
- старый формат [width, depth] + height поддерживается;
- массивы длиной 1 и отсутствующие данные имеют безопасный fallback;
- mission_02.json намеренно оставлен в старом формате, чтобы настоящий runtime-smoke проверял обратную совместимость;
- добавлен test_v196_mission2_house_compat.py.

Исправление тестового контура:
- четыре файла с top-level test_* раньше не подхватывались командой unittest discover;
- добавлен load_tests(), поэтому эти 21 тест теперь действительно выполняются;
- итоговое число реально выполненных Python-тестов: 164 вместо 143.

Локально выполнено успешно:
- 164 Python-теста: OK;
- синтаксическая компиляция 38 Python-файлов: OK;
- разбор 36 JSON-файлов: OK;
- проверка 354 буквальных ссылок res://: OK;
- проверка целостности 555 PNG: OK;
- разбор двух GitHub Actions workflow: OK;
- continue-on-error отсутствует;
- обязательные этапы импорта Godot, runtime-smoke миссий I–VI и Windows-export сохранены блокирующими.

Ограничение локальной среды:
- исполняемого Godot 4.6.3 в контейнере нет;
- фактический импорт изменённого проекта, runtime-smoke и Windows-export должны быть подтверждены новым запуском GitHub Actions;
- в предоставленном логе импорт Godot версии 1.9.5 прошёл, а сборка была корректно остановлена именно runtime-ошибкой миссии II.
