Сборка 1.9.10 подготовлена по свежему логу logs_82037651950.zip.

Исправлена точная причина зависания миссии VI: неверное определение запуска --headless через OS.has_feature("headless"). Теперь используется DisplayServer.get_name() и явные smoke-параметры.

Проверено локально:
- 198/198 Python-тестов;
- Python/JSON/PNG/res:// целостность;
- YAML обоих GitHub workflow;
- bash-синтаксис всех 38 run-блоков;
- отсутствие continue-on-error;
- обязательные Godot import, миссии 1–6, обе ветки VI и Windows export.

Настоящий запуск изменённого проекта Godot 4.6.3 и Windows-export должен подтвердить GitHub Actions, поскольку исполняемого Godot в локальном контейнере нет.
